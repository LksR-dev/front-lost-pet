import { useEffect } from 'react';
import { useParams } from 'react-router-dom';
import { searchProductState, queryState, itemId, itemIdState } from '../atoms';
import { useRecoilValue, useRecoilState } from 'recoil';
